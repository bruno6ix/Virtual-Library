<header>
    <navbar class="navbar">
        <div class="search">
            <form action="/resultado" method="GET">
                <input type="search" placeholder="Buscar" name="Buscar">
                <button type="submit">Buscar</button>
            </form>
        </div>
        <div class="login">
            <% if (isLoggedIn) { %>
                <a href="/">Inicio</a>
                <a href="/book-create">Nuevo Libro</a>
            <% } else { %>
                <a href="/login">Iniciar sesión</a>
            <% } %>
        </div>
    </navbar>
</header>
